# Master CV inventory — facts, voice, and editable zones

The master CV (`Alex Orlov_new.docx`, Google Drive id `16ZB7bDnjzpmpE0wnU9wq6pSOu1aS0c60`, in the
`CVs` parent folder; the **Custom CVs** output folder id is `1ykh5bp4QMf6W0lftJwVnUpAdWG6RfBdF`).
Always re-download the master fresh each run — Alex edits it.

**Layout:** 3 pages, A4, built as 2 Word tables (no manual page breaks). A wide **left column**
(About, Experience, Education, talks) and a shaded **right sidebar** (name/title header, Details,
About companies, Skills). The left column drives pagination; section headings sit right at page
bottoms, so **any line-count change in the left column ripples across all 3 pages.** Treat the
3-page length as fixed — never try to compress or expand it.

---

## Defensible facts (what Alex can defend cold — the only material tailoring may surface)

**Headline identity:** Product executive, technical background (M.Sc. Systems Analysis / CS-DS),
14 years in software products. Title line reads: *"CPO, VP of Product, Director PM, Head of Product."*

**Now — Fractional / interim CPO, agentic-AI B2B startups & enterprises (Jul 2024 →):**
- *Code Maestro* (AI agent for Unity dev): owned product concept→MVP→early traction; first 25
  activated customers; 60%+ 6-month retention; led PM/SME/eng hiring; set up AI product system
  (evals, experiments, prototyping); pilots reported 2× faster dev onboarding, 1.2–3× productivity.
- *SoftServe R&D* (enterprise agentic engineering products): positioning, roadmap, execution for 2
  products w/ 2 PMs → MVP + first 5 enterprise customers; 25% QA productivity gains (100+ QA teams),
  60% faster migration delivery.

**Jooble — CPO (Innovation), Apr 2022 – Jun 2024:** built new-products division from zero; product
expansion strategy; incubated 9 WorkTech startups; directed $3M across them in 14 months; set up
ideation/validation/PMF processes; acted as EIR on 2 validations (genAI assessment tool, comp-mgmt SaaS).

**Creatio — SVP Product & Ecosystem, Apr 2018 – Mar 2022 (GM role, reported to CEO):** built
product+GTM teams (30+); owned unit P&L; grew a no-code app-building platform + B2B SaaS app
ecosystem 0→750+ apps, 2000 monthly installs, 200+ devs; drove category shift from CRM to no-code
platform; unified marketplace CI/CD, composable architecture, APIs → 50% faster app TTM; new line of
business 0→10% of company ARR in 2 yrs; scaled enabling-products team 5→20; launched e-learning
platform/SDK (60% adoption), revamped docs & user community.

**Creatio — Director of Product Management (CRM), Mar 2016 – Apr 2018:** portfolio of sales/marketing/
service CRM; mentored 6 PMs; adopted customer-feedback/continuous-discovery → NPS above industry avg;
launched ML engine in CRM (case classification/routing, predictive scoring, next-best-action; exposed
via no-code) → 30% L1 support cost reduction, up to 10% cross-sell uplift (retail banking); intro'd
free trials + product analytics (product-led motion).

**Creatio — Head of Product / Product Leader (MarTech), Apr 2014 – May 2017:** full-cycle head of
product for a B2B SaaS multichannel marketing-automation & lead-mgmt platform; grew it 0→400+
enterprise customers and Gartner MQ leadership; vision/roadmap, continuous discovery, feature design,
delivery (Scrum PO for 2 teams), co-led product marketing & pricing, public evangelism, managed APOs.

**Earlier:** Creatio Project Delivery Manager (2013–14, enterprise CRM implementations);
Paymentwall PM/Product Analyst (2011–13, FinTech; partnerships/integrations; 100+ monetization tests).

**Companies (context, true):** Creatio (no-code workflow-automation & CRM unicorn, $100M+ rev, Gartner/
Forrester leader); Jooble (global job-search platform, top-10 worldwide); Paymentwall (global payments).

**Education:** MIM Business School (Mgmt & Leadership, 2019–20); M.Sc. Systems Analysis, NTUU (honors,
2007–13); Reforge (Growth/Product Strategy/PMF/AI Strategy 2021–25); LvBS, ASU strategic negotiations,
UXPresso HCD, Coursera AI PM.

**Talks/teaching:** ProductCamp, ForbesTech; Laba course author/lecturer (PM foundations);
Projector / PartnerUP mentor; Intelligent People mentor.

---

## Differentiation & voice (preserve — this is what must NOT be sanded away)
- **Range with depth:** Head of Product → Director → SVP/GM → CPO → fractional. A builder who scaled
  (Creatio $10→80M) *and* a 0→1 operator (Jooble innovation, agentic-AI startups).
- **Platform/ecosystem + GM/P&L** ownership, not just feature PM. Owned a P&L and a new revenue line.
- **Agentic-AI-native, hands-on** ("AI context engineering", evals, prototyping) — rare at exec level.
- **Tech-heavy** product leader who can sit with engineering/architecture.
- Voice markers: the **◆-tag** taxonomy, the "Sweet spots" framing, "Efficient growth (doing more
  with less)", "vision and systems from uncertainty and turbulence", quantified outcomes with named
  systems. Keep the dry, specific, metric-anchored register. Non-uniform bullet rhythm is a feature.

---

## Editable zones (in safest-first order) — verbatim current content

### 1. Skills list — sidebar, 3 groups divided by `——————` (SAFEST: reorder = zero length change)
Each skill is its own paragraph (vertical list), so **reordering items changes nothing about total
line count** → zero layout risk. Reorder *within a group* first; cross-group moves change the
visual grouping so do them only with reason.
- **Group 1 (core):** Leadership and Management · Product Management · Workflow automation · GTM Strategy
- **Group 2 (methods):** Business Analysis · Project Management · UX Design · Business Process Management · SDLC, Agile Methodologies · Martech, CRM, ERP
- **Group 3 (technical):** Enterprise Architecture · Data Management & Integrations · SQL · No-code · AI / ML Models · AI context engineering · Agentic AI platforms · AI coding

Rules: reorder to lead with JD-relevant skills. You may swap one term for the JD's exact synonym of
the **same true skill** (e.g. "GTM Strategy" → "Go-to-Market Strategy" if the JD spells it out) — keep
width similar. Never add a skill Alex can't demonstrate; never add/remove list items (changes line count).

### 2. Industry-experience ◆-tags — inline, left column (reorder freely; swap term-for-term ≈equal width)
Current order: `CRM ◆ Marketing automation ◆ No/Low-code platforms ◆ Workflow automation ◆ BizOps ◆ HRTech ◆ Agentic AI`
Reorder so the JD's domain leads. A term swap must be near-equal character width (these wrap inline —
a longer term can push a wrap). All are TRUE domains Alex worked in.

### 3. Sweet-spots ◆-tags — inline, left column (reorder freely; ≈equal-width swaps)
Current: `Enterprise B2B SaaS ◆ Platform products ◆ Agentic AI B2B products ◆ Tech-heavy products ◆ GM-like roles ◆ New products & product expansion ◆ Efficient growth ("doing more with less") ◆ Vision and systems from uncertainty and turbulence`

### 4. About / summary paragraph — left column, top (re-lens emphasis; LENGTH-NEUTRAL prose swaps)
Re-point which capabilities lead to match the role's mandate. Swap adjectives/clauses for
≈equal-length ones; do not add or remove sentences (each wrap shift risks the whole page cascade).

### 5. Headline title line (under name): "CPO, VP of Product, Director PM, Head of Product"
You may reorder to put the role's title first if it's in the list and TRUE. Don't add a new title.

### 6. Experience bullets (left column) — HIGHEST layout risk; edit last & verify hardest
Re-phrasing the same fact in the JD's vocabulary is allowed, but ONLY if the new text wraps to the
exact same number of lines. Reordering bullets within a role is safe IF each bullet is its own
paragraph and the set is unchanged. Prefer NOT to touch bullets unless the alignment gain is real and
you can hold the line count. Adding/removing a bullet is effectively forbidden (cascades pagination).

---

## How to edit this specific doc (run structure)
- Runs are fragmented (bold vs. non-bold split across `<w:r>`). To swap a word, Edit the exact
  `<w:t ...>TEXT</w:t>` substring, preserving the surrounding tags and the run's `<w:rPr>`.
- The ◆ separators live inside `<w:sdt>…<w:sdtContent>` wrappers; the terms are plain `<w:r>` runs
  between them. **To reorder ◆-tags**, move the *term run text* — keep the ◆ `<w:sdt>` blocks in place.
- Skills items are separate paragraphs; to reorder, swap the `<w:t>` contents of two skill paragraphs
  (leave the paragraph shells alone) — this guarantees identical line count.

### Reordering = a 3-step sentinel swap (NOT a naive two-step) — avoids the twin-collision trap
Swapping two items' text in two Edits creates a moment where both `<w:t>` read the same string, and
the Edit tool then can't target the second one uniquely (it errors on a non-unique match). The
**wrong** fix is to fall back to a whole-file Python rewrite — that re-serializes the entire
`document.xml` and silently re-encodes smart quotes to numeric entities across *untouched*
paragraphs, which blinds the layout diff gate. **Always reorder with a unique temporary sentinel:**
1. Edit item A's `<w:t>` text → a unique sentinel, e.g. `@@SWAP@@`.
2. Edit item B's `<w:t>` text → A's original text.
3. Edit `@@SWAP@@` → B's original text.
Each `old_string` is unique at the moment you run it, so the Edit tool never collides and every
untouched run stays byte-identical. (For multi-item rotations, sentinel each moved item the same way.)
**Never** rewrite the whole `document.xml` with a script. If you somehow must, serialize with the
master's exact quote/entity style so unedited runs stay byte-for-byte unchanged.

### Length-neutral About re-point (the high-value, higher-risk lever) — SWAP, don't ADD
The About paragraph is `jc="both"` (justified) in the pagination-driving left column, so adding words
risks a 3-page cascade. To re-point it safely: find an existing clause and replace it with a
JD-aligned, TRUE clause of **equal-or-shorter** character count, then rebuild and confirm the wrap +
page count held. Do not append. Example moves (illustrative — verify each):
- Lead clause "…launching, achieving product-market fit, and scaling global B2B SaaS products." For
  an agentic-AI role, "scaling global B2B SaaS products" → "scaling global B2B SaaS and AI products"
  is ~length-neutral and TRUE (his current work is agentic-AI) — but it's slightly longer, so verify
  the wrap; if it adds a line, revert. Prefer swapping an adjective for an equal-width one over
  inserting a noun.
- If no equal-width swap surfaces the mandate without echoing the JD or reading forced, **leave the
  About alone** — the first experience block already carries the through-line. A clean skip here,
  with the mandate surfaced via the safer levers, still scores 9-10.

### Hygiene
- Smart quotes: use XML entities for new apostrophes/quotes (`&#x2019;`, `&#x201C;`, `&#x201D;`).
- Keep `xml:space="preserve"` on any `<w:t>` you touch that has leading/trailing spaces.
- After editing, diff the post-edit `document.xml` against a copy of the pre-edit version and confirm
  ONLY the runs you intended changed (no stray re-encoding). The repack is a naive re-zip, so any run
  you didn't touch with the Edit tool stays byte-identical — a clean diff is the proof.

---

## Positioning constraints (from context/areas/job-search/positioning.md)
- **Never introduce Ukraine / Kyiv** anywhere. (The master's "National Technical University of
  Ukraine" education line is PRESERVED as-is — it is not a tailoring target; do not add more.)
- Position as San Francisco-based, work-authorized. Never mention visa/relocation status in the CV.
- US audience → English only.
- Target roles: B2B SaaS product leadership (esp. workflow-automation / CRM / BizOps), ~$5–30M rev,
  key product leader or a level below VP/CPO in larger orgs. Sweet spots: enterprise B2B, agentic AI
  for enterprise, platform/ecosystem, new products & expansion, scaling the org, GM-like, efficient growth.
