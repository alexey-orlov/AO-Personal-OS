# Other — unrouted calls

Catch-all area for calls the classifier couldn't map to a real area, plus junk (mic tests, empty clips). Not a project — no live-state sections.

- `context-update` junk-gates test recordings and routes anything substantive to the right area page.
- If a topic recurs here, promote it: new area folder under `context/areas/` + a new Axis-2 context in `.claude/skills/classify/SKILL.md`.
