# default — 2026-08-12_224607
_source: 20260811 171152-3BA77978.m4a_

> _No calendar event matched recording start 2026-08-11 17:11:52 EEST._

## TL;DR
- Speaker A will help Vladislav set up Azure DevOps API integration today; deadline is tomorrow.
- No pre-built Azure connector exists in Claude Code (unlike the GitHub one), so it's a manual/API-based setup using Speaker A's existing Azure DevOps skills.
- A working session is set for right after the ~15-minute meeting with David (~5 PM CET), as a sidebar.

## Key points
- Existing setup uses the pre-built GitHub connector (UI-configured); there is no Azure connector, so Azure DevOps requires back-and-forth via the API.
- Speaker A built a set of skills wrapping the Azure DevOps API for personal use, then realized they'd be useful to others; may have a Confluence article on it (unconfirmed).
- Access model: an Azure DevOps PAT / API token inherits the user's existing repo permissions — whatever a user can do in the front end, they can do via the API.
- Speaker B assumed extra access rights would be needed for API/integration beyond the Git pull/push user; Speaker A corrected this — no separate rights, but the user must create the PAT themselves under Azure DevOps user preferences.
- The skill expects the token to be available in the environment/path; from there Claude follows pointers to what it needs.
- Azure DevOps API is already in use for non-code purposes: a metrics plugin pulls Azure DevOps-specific data the same way.
- Current access status unclear — as of yesterday it wasn't granted; Vladislav to verify.
- Scheduling: Speaker A's Outlook calendar is generally accurate for availability. A "daily" meeting showed as canceled; David has a meeting in ~15 minutes.

## Decisions & commitments
- Speaker A -> help Vladislav set up the Azure DevOps API integration on a live call -> today, after the meeting with David.
- Speaker A -> share the Azure DevOps skills (and Confluence article, if it exists) with Speaker B's team -> today (inferred timing).
- Speaker B -> reach out to Vladislav (currently in another meeting) about the setup and the session -> today.
- Vladislav -> create his own Azure DevOps PAT under user preferences and confirm repo access -> before/at the session (inferred).
- All -> hold the Azure setup sidebar right after the David meeting (~5 PM CET, or earlier if that call ends early) -> today.

## Open questions / follow-ups
- Whether Vladislav actually has Azure DevOps repo access yet (not granted as of yesterday) — he was to check.
- Whether the Confluence article on the Azure DevOps API skills exists / where it lives.
- Whether David is available for the sidebar right after his meeting; if not, schedule immediately after.
- What exactly to request from the client in terms of access rights, if it turns out anything beyond standard repo access is needed.
- How the skills behave in Vladislav's environment — troubleshooting expected during the live session.
