# sales-call — 2026-06-16_191926
_source: 20260616 183115-6E88BEC4.m4a_

> _No calendar event matched recording start 2026-06-16 18:31:15 EEST._

## TL;DR
- Technical sync between Bosch and Oracle/NVIDIA teams on cuOpt POC issues: GPU node 2 throws errors (kube 500 / abort timeout) under load while GPU node 1 handles the same payload fine.
- Working hypothesis is either a faulty GPU on the second device or a config/indexing artifact from how jobs are being pinned; a debug path was agreed without disturbing Bosch's ongoing UAT on GPU 1.
- Bosch tenancy expiry confirmed as 2026-08-26, removing the immediate risk of dropping to free tier mid-POC; other production-deployment topics deferred to a follow-up.

## Participants & roles
- Speaker A — NVIDIA/Oracle technical engineer, ran cuOpt POC benchmarking (inferred).
- Speaker B — cuOpt/Kubernetes specialist on the Oracle/NVIDIA side, owns API server and infra guidance (inferred).
- Speaker C — Bosch engineering lead on cuOpt tuning (Pavel/Pawel referenced; identity ambiguous) (inferred).
- Speaker D — Bosch project member, also owns RIA Tier 1 POC context (inferred).
- Speaker E — Oracle/NVIDIA account or solutions lead facilitating next steps (Vishnu) (inferred).
- Speaker F — Bosch delivery lead driving the agenda (Paweł, author of the pre-call email) (inferred).
- Speaker G — Oracle account-side contact (Kenan) coordinating tenancy extensions (inferred).
- Speaker H — Oracle account team, holds tenancy expiry data (Saad or delegate) (inferred).

## Customer context
- Bosch is at the end of a cuOpt POC ("bull run"/UAT phase) on an OCI AI Accelerator Pack (AIPAC) deployment, with a single OKE worker node carrying two A10 GPUs.
- Customer testing is live on GPU 1; team is doing fine-tuning rather than new features, and is sensitive to anything that could disrupt the ongoing Bosch test cycle.
- A parallel RIA Tier 1 POC previously hit a tenancy auto-downgrade to free tier that broke CI/CD; Bosch wants to avoid a repeat.
- Bosch tenancy was recently extended; current end date is 2026-08-26 (~10 weeks of runway from call date).

## Objections & risks
- GPU 2 fails on identical payloads that GPU 1 runs cleanly (kube "server-side abort timeout, not retrying") → Oracle proposed PyTorch hello-world per GPU, `nvidia-smi`, `sudo dmesg` for xid/memory errors, then node rebuild if confirmed broken → **open**.
- Risk that the failure is a config artifact (wrong GPU index after manually disabling one) rather than a hardware fault → Bosch to reproduce without manual disabling and share logs/payload; Oracle to attempt repro on their side → **open**.
- Node rebuild could disrupt active Bosch UAT on GPU 1 → Oracle suggested bringing a second A10-2 online first then killing the failing node to keep downtime ~1 minute → **mitigation proposed, open**.
- Payload >2 GB / >~7,500 stops can exceed cuOpt limits and fail (Speaker A's benchmark slide) → Oracle/NVIDIA team judged this is not the current symptom since GPU 1 handles the same payload, but Bosch to verify payload size → **likely not the cause, open verification**.
- Tenancy could auto-revert to free tier and break deployment again → Confirmed extension to 2026-08-26; account team will extend further on request → **resolved for now**.
- Performance tuning trade-offs (presolve enable/disable, FP64 default) flagged as worth revisiting → Speaker A to share docs / suggest FP32 (~4% perf gain) and geographic clustering → **open**.

## Commitments & next steps
**Bosch (theirs):**
- Speaker F / Speaker C — collect and forward full logs and setup details from George for the GPU 2 failures — no firm date, ASAP.
- Speaker C — check whether the payload stays under the cuOpt stop-count / 2 GB limit and confirm the presolve config in use — no date.
- Bosch team — run a small PyTorch test against each GPU (via SSH or debug pod), plus `nvidia-smi` and `sudo dmesg`, to isolate whether GPU 2 is physically faulty — targeted for the following morning.
- Speaker F — share a failing cuOpt request payload with Oracle so they can repro independently, in a way that doesn't disturb Bosch UAT on GPU 1.
- Speaker D — coordinate a deep-dive session with George on screen-share later in the week if logs aren't conclusive.

**Oracle / NVIDIA (ours):**
- Speaker E — schedule the deep-dive technical session with George once Bosch has logs ready.
- Speaker A — send by email the cuOpt tuning notes: parallelization technique, FP32 vs FP64 trade-off, geographic clustering, and the presolve-behavior answer; optionally a follow-up 1:1 with Speaker C.
- Speaker B — once payload is received, attempt to reproduce the failure on Oracle-side cuOpt API server; help design a debug path that avoids touching the live Bosch GPU 1; validate the "bring up second A10-2, then drain" rebuild plan in their tenancy.
- Speaker G / Speaker H — already confirmed Bosch tenancy is extended to 2026-08-26; remain on standby to extend further if Bosch requests it.

## Open questions
- Is GPU 2 actually defective, or is the failure an artifact of how George is pinning jobs / indexing GPUs after manual disable?
- What exact errors and stack traces are in the GPU 2 logs beyond the truncated 3-line snippet George shared?
- What is the size (stops, payload bytes) of the failing job, and does it cross the 2 GB / ~7,500-stop cuOpt threshold?
- Does cuOpt's presolve setting interact with the slide's payload limits, and is that contributing to the bigger-file failures?
- Can the worker node be rebuilt with zero impact to active Bosch UAT, and what's the exact procedure to do so on AIPAC?
- The other production-deployment and performance topics from Speaker F's pre-call email — deferred, need a follow-up slot.

## Notable quotes
- Speaker C: "We submit one job. When we submit on GPU 1, it doesn't fail. When we submit it on GPU 2, it does fail. The same job."
- Speaker B: "What I'm worried about here is that you may actually have a broken GPU."
