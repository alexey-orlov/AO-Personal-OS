# D — oracle-packs-web (listing + demo) vs Oracle-Solutions-Site: cross-repo duplication audit

Date: 2026-09-24. Read-only audit. Repos:
- Plugin: /home/user/oracle-packaging-skills (plugins/oracle-packs-web/skills/{listing,demo}, shared/)
- Site: /home/user/oracle-solutions-site
- Personal: /home/user/AO-Personal-OS/.claude/references/

Status: in progress (sections appended as each part completes).

## Part 1 (in progress) — listing skill vs the site's self-description

Site sources read: site.manifest.json, CLAUDE.md, docs/START-HERE.md (whole), docs/SCHEMA.md (whole), docs/CONFIG.md (whole), tools/check-grammar.js (constants 1-130, products 130-260, demo 860-890, durations 1649-1686, names 2000-2207).
Plugin sources read: listing SKILL.md, cards/manifest.yaml, all 16 cards, listing-schema.md, listing-rules.md, preview-and-publish.md.

### Checker facts (tools/check-grammar.js) — the site's real contract
- L153: `C.products.length !== 7` fails -> "expected exactly 7 products". Final line L2207 prints "OK — 7 products". An 8th product can never pass Gate 1 without editing the checker.
- L96: `DEMO_SLUGS = [large-document-extraction, workforce-optimization, cross-system-erp-qa]`; L883 fails any product whose links.json interactiveDemo is set but is not in DEMO_SLUGS. The inserter's `--demo-path` therefore fails the checker for any new walkthrough.
- L99: `UNPACKAGED = [case-evidence-collection, plan-vs-actual-investigation]`; L184 fails statusNote on any other slug. entry-identity card: "statusNote only where no package exists yet" -> a new unpackaged pack fails.
- L1656-1685: POV = "4–8 weeks" hard-coded: durationShort, investment.duration, promise must contain it; pillars[fast] must match /4–8 weeks|Four weeks/; raw regex bans "duration is set at scoping", "12 weeks", etc.
- L101: `CUSTOMER_NAMES` hard-coded, 13 names. The checker reads NO deny-list file (grep "deny" in check-grammar.js: 0 hits; whole site: only manifest paths.denyList, .gitignore, prose).
- L50 NEXT_TIERS = ["Integration","Scaling"]; L522 title === "Jumpstart Proof-of-Value"; L53 CAP_STATES = supported/partial/roadmap; L32 INDUSTRIES = the 16 keys; L78 FACET_LABELS short labels.

### Drift found so far (plugin side vs site)
1. Deny-list gate (gates card #1, preview-and-publish §3, denylist-to-json.py docstring, deny-list.example.json): plugin says "the customer-name gate fails open: with no list configured the checker warns and exits 0" and "check-grammar.js warns on every run until customerNames is non-empty". FALSE vs site: check-grammar.js never reads tools/deny-list.json and has no such warning; it uses its own hard-coded CUSTOMER_NAMES. The generated JSON is read by nothing. -> ALREADY DRIFTED.
2. Name coverage: shared/tools/denylist.txt = 19 names (+GigaCloud, AIDP, logos/, assets/img/logos/). check-grammar.js CUSTOMER_NAMES = 13; missing Sky, Nesma, Toyota, FreeTech, NATO, Eriks. START-HERE §6 gate-3 grep = `bosch\|riyadh\|dhl\|sbg\|logos/` (4 names). Three lists, three sizes. -> ALREADY DRIFTED (site side lags the practice list).
3. PoV duration (entry-jumpstart #3/#4, listing-schema §5 + §10 row `jumpstart.durationShort <- packages.tiers[pov].duration_weeks`; "price and duration may each be null"; promise "otherwise the sentence says the scope is agreed at scoping"): site checker requires "4–8 weeks" literally in durationShort, investment.duration and promise; null duration fails; "duration is set at scoping" is a banned phrase. -> ALREADY DRIFTED (plugin implies spec-derived duration and allows null; site hard-codes 4–8 weeks). Note the site's own SCHEMA.md L297 still says "price and duration are each a string or null" — the site doc lags its own checker too.
4. Product count / demo slugs / unpackaged slugs: no plugin file mentions that the site checker hard-codes 7 products, 3 demo slugs, 2 unpackaged slugs. The listing skill's core job (add a product) cannot pass "Gate 1 — the site's checker" as written. -> GAP (plugin procedure cannot succeed without a site-checker edit it never names).
5. Heading budgets (copy-rules #4, listing-rules B4): "H1 two to four words, ≤ ~24 characters a line, ≤ 2 lines". START-HERE §4 design table: that is the ARCHIVE theme's budget; the live SS26 site is "H1 ≤ 15 chars a line × 2 ... product name ≤ 22 × 2; H2 ≤ 30, now enforced". -> ALREADY DRIFTED.
6. Theme rules (listing-rules C9): "One ground, one accent per screen ... at most one light band per page". Live SS26: white ground, at most ONE DARK band, two accent roles (#1485c4 act/selected, #f46a4a one hero accent line). -> ALREADY DRIFTED (plugin describes the archived near-black theme).
7. Overview tab composition (entry-overview card, listing-schema §2, listing-rules C3): "problemSolution → steps → industryCases as tabs → metrics in a side rail"; caseStudy at the foot of Overview. Site round 10 (START-HERE §3, SCHEMA L269): tabs are Overview · Use cases · Technology · Jumpstart · Contacts; industryCases and caseStudy render on the Use cases tab. Data keys are unchanged (still overview.*), so the mapping still works; the rendering description is stale. -> DRIFTED (descriptive only).
8. Platform chip (claim-rules #6, listing-rules A6, entry-identity #2): "the vendor's own product name, verbatim and unabbreviated" / "spelled the vendor's way, never abbreviated". Site round 9: tags[1] is the SHORT label ("AI Lakehouse", "OCI + NVIDIA NeMo"), checker L215 FACET_LABELS; full Oracle name only on Services cards and prose. listing-schema §9 ("tags[1] === the facet's label") is consistent; the prose rule contradicts it. -> DRIFTED (rule wording).

### More drift (Part 1, continued)
9. `media[slug]` gap (insert card: "Nothing else is touched — `site`, `media`, … belong to whoever owns the site"; listing-schema top: same list). Site: `UI.figure(key)` in site/assets/app.js L429-456 returns "" unless `SITE_CONTENT.media[key]` exists; content.js L42-68 holds `media: { <slug>: { diagram: <slug>, alt } }` for all seven. So the figure the skill generates into diagrams.js never renders on the Technology tab of a NEW product, and its alt text has no home. -> GAP (plugin forbids the one extra key the site needs).
10. Tier vocabulary vs the plugin linter: site checker L522 requires `jumpstart.title === "Jumpstart Proof-of-Value"`; tab label "Jumpstart"; SCHEMA cta "Start a Jumpstart conversation"; START-HERE §4 "Jumpstart proof of value · Integration · Scaling". Plugin shared/tools/lint_artifact.py TIER_RULES ART402 `(?<!PoV )\bJumpstart\b` ("the tier is `PoV Jumpstart`"); shared/references/anatomy/wfo-divergences.md L8 decides "`PoV Jumpstart / Integration / Scaling` everywhere". EMPIRICAL: `lint_artifact.py assets/exemplar-product-entry.js --channel customer_site` -> exit 1, ART402 `Jumpstart` x14, ART301 x4 (price footnote >200 chars away in the data file). The site's own exemplar (checker OK) fails the plugin's step-6 lint. The listing "Done" (three gates green AND lint clean) is unreachable at the exemplar's altitude. -> ALREADY DRIFTED (a practice-vs-site vocabulary conflict, not just doc lag).
11. Exemplar vs "only the PoV price ships" (entry-jumpstart #2, listing-rules A8, assets/README calibration point): the exemplar ("the spec") L590 carries Integration "€300–500K services · ~€25K/mo infrastructure". START-HERE §2 lists this as a known brief mismatch ("Two products print an Integration price"). Plugin rule text hedges ("only where a signed-off source publishes one"), so not strictly contradictory, but the exemplar teaches the exception. -> TENSION (flag, owner decision pending on the site side).
12. Site-internal lag worth knowing (not plugin drift): SCHEMA.md L278 says `state` is `supported` or `roadmap` (checker CAP_STATES also allows `partial`); SCHEMA L298 says next is "Integration then Scale" (checker NEXT_TIERS "Scaling"); SCHEMA L39 says nav has three items, L373 says two; CONFIG §5 still lists "at-a-glance side facts", "four-step flow", "POV fact strip" (retired); CONFIG §3 successStoryUrl says "Overview tab's case study" (moved to Use cases in round 10). The plugin is CONSISTENT with the checker on state/tiers — here the plugin is right and the site's prose is stale.

### Consistent today (restated, no drift found)
- Config switch block: 5 keys marketplace/marketplaceUrl/video/videoPoster/successStoryUrl, booleans real, marketplaceUrl-without-marketplace fails (switches card = CONFIG.md §1/§3 = checker L221-245).
- links.json: six keys in order, "" until the artifact exists, syncLinks after a change, retired demoUrl/demoPreviewUrl/videoUrl/materials/sellers (switches card, listing-schema §6 = CONFIG.md §3a).
- tags exactly two, tags[0]===categoryChip, tags[1]===facet label (listing-schema §9 = checker L208-218).
- contactPerson required and in shared.people (entry-identity #6 = SCHEMA L203 = checker; inserter refuses too).
- Hero slot order (entry-identity #4 = SCHEMA L219 verbatim).
- 16 industries (entry-overview, listing-schema §13 = checker INDUSTRIES L32-37).
- Case study keys, status set, retired keys (entry-case-study = SCHEMA overview.caseStudy).
- Technology: two blocks, stack 4-5 layers in fixed order, capabilities exactly 4 stages ≥3 items, retired keys (entry-technology = SCHEMA technology = checker STACK_KEYS/CAP_STATES).
- Jumpstart title, pillars fast/low-risk/tangible, next Integration→Scaling, packaging disclaimers banned (entry-jumpstart = checker L50/L46/L522/L1518-1521).
- Preview widths 1440/1280/1024/768/375 + H1 at 320; origin 127.0.0.1:8765 not localhost; fresh-assets trick; scroll-0 screenshots (preview card, preview-and-publish §1-2 = manifest.preview + START-HERE §6).
- Publish: exact-line strip list (preview-and-publish §4a copies the manifest's 9 lines verbatim — consistent today but a duplicated constant), target/neverPublishTo, files map, list_files confirm, walkthrough standalone (publish card = manifest.publish + START-HERE §6).

## Part 2 — the exemplar
- `node tools/refresh-exemplar.mjs --site /home/user/oracle-solutions-site --check` -> "OK — matches the site's workforce-optimization at contract round 14". Stored copy is current today (699 lines, 33 KB).
- What the tool does beyond copying (refresh-exemplar.mjs header L15-31 + code): (a) evaluates brand.js + content.js + content-case.js overlay + config.js + diagrams.js in one sandbox, so strings read as rendered — the overlay DOES touch the exemplar: content-case.js L64-67 re-cases products[4] (workforce-optimization) headline.accent "WORKFORCE", headline.rest "OPTIMIZATION", problem title "THE PROBLEM", solution title "THE SOLUTION: REVIEW THE PLAN, NOT BUILD IT"; (b) blanks 5 links.json values (onePager, salesDeck, featureList, interactiveDemoArtifact, video) — note assets/README.md L22 says only two are blanked (intra-plugin drift); (c) refuses to write if the output holds a denylist.txt name; (d) `--check` drift mode used by shared/tools/tests/run_tests.sh L1005-1142 (live check = WARNING only, plus scratch-site cases).
- Does any of that matter if the skill reads the site at run time? (a) matters: a raw read of content.js shows 4 capitalised strings a builder would copy — so a run-time read must evaluate content.js + content-case.js (what index.html does), or the site must finish its sentence-case migration (START-HERE §1: 41 rows, "shrinking every round"). (b) only matters for a copy stored in the plugin repo; a run-time read happens inside the owner's site checkout, where links.json is already on disk. (c) only protects the plugin repo from carrying a name; at run time the site's checker + Gate 3 sweep + lint ART001 cover the entry. (d) disappears with the stored copy.
- Verdict: the stored copy + refresh tool + drift test can go. Replace with a run-time read of `manifest.exemplarProduct` through a sandbox that loads content.js then content-case.js (a ~10-line helper, or better a site-side `tools/print-product.js <slug>` so the site owns its own serialisation). Keep the exemplar-altitude card (plugin-only guidance on HOW to read it: fresh-context agent, key by key).

## Part 3 — gates duplicated between plugin and site
- Deny-list: three lists. shared/tools/denylist.txt (19 names + GigaCloud, AIDP, 2 path fragments) -> read by lint_artifact ART001/ART002, lint_spec SPEC010, refresh-exemplar. Site check-grammar.js CUSTOMER_NAMES (13) + banned strings (GigaCloud, AIDP, €-figures, …). Site START-HERE §6 grep (4 names + logos/). listing/tools/denylist-to-json.py + deny-list.example.json + manifest paths.denyList + site .gitignore entry = orphans of the retired plugin checker port (git: 4ad2fb6 port read `$ORACLE_DENY_LIST`/`tools/deny-list.json` `{customerNames, bannedStrings}` and warned "the customer-name gate is not running"; a48e92a deleted the port, kept the converter). Verified: tools/deny-list.json absent in the site checkout, checker run -> "OK" with 3 warnings, none about a deny-list.
- Fix direction (inferred): either the site checker reads the practice list (manifest paths.denyList, git-ignored, generated) and fails closed when absent — then the converter is load-bearing; or delete converter + example + gate-1 text + paths.denyList and let lint_artifact ART001 (19 names) be the plugin's name gate on the entry file, with Gate 3 grep for the whole publish root. Either way the "fails open → treat the warning as a failed gate" instruction must go: that warning no longer exists.
- lint_artifact --channel customer_site vs check-grammar.js: overlapping rule families (names, AIDP, counts/totals, negations, packaging vocabulary, tiers) with DIFFERENT lists and scopes: checker applies per key (e.g. PACKAGING_PHRASES only on oneLiner; count ban only on 6 home/catalog keys) and is authoritative for structure; lint applies regexes over the whole file. Real conflicts: ART402 vs the mandated "Jumpstart Proof-of-Value"; ART301's 200-char proximity vs a data file where the footnote is a sibling key; ART302 (non-PoV price) is stricter than the site, which ships an Integration price. Complementary: ART001 has 6 more names than the checker; ART201/202/203 cover keys the checker does not.
- Verdict (inferred): keep lint_artifact for the entry file only for what the site checker cannot know (spec-backed rules: ART104 name variant, ART206 one-liner, ART302 price vs spec, ART401 proven vs figure_status, ART001 with the full list); add a `customer_site` exemption for the site's mandated tier title or change the site; do not re-implement structural checks.

### Tools that bake in site knowledge (Part 1/3 addendum)
- diagram_to_site.py FIT table: "measured on the renderer's own geometry (diagrams.js: source 236 px, group node 312 px, target 248 px)". Site diagrams.js L155-158: leftW 236, rightW 248, innerW 312 — CONSISTENT today, but a copied constant with no check (check_diagram.py compares figure vs model, not vs renderer geometry). Verdict: keep plugin-only (the transformation from architecture.json is plugin logic); note the coupling.
- derive-stage-view.py: pure spec -> stage view; plugin-only. Its "exactly 4 stages, ≥3 items" invariants mirror checker rules; fine as a pre-check.
- insert-product.mjs: plugin-only splice/refusal mechanics; site-shape assumptions (products: [ … ], config products: { … }, productOrder, links.json products) all consistent today. Missing: media[slug] (see #9); unaware of the checker's hard-coded 7/DEMO_SLUGS/UNPACKAGED (see #4).

## Part 4 — demo skill vs site and the personal playbook
- reference-demo/ = BYTE-IDENTICAL copy of site/demo/workforce-optimization/ (`diff -rq`: only README.md differs — it exists only in the plugin). ~175 KB of site code (index.html 16 KB, demo.css 46 KB, demo.js 86 KB, data.js 27 KB per its README).
  Verdict: read from the site at run time (`<site>/<paths.demos>/<manifest.exemplarProduct>/`), keep only the README's "what to look at" table as a card. Risk: the site copy can change (the plugin README says "kept whole, never rewired"; the site may evolve it) — acceptable because the site copy IS the reviewed, shipped build.
- tour-engine.js: NOT a copy. No site file references it (grep tour-engine|TourEngine in site: 0). The three site demos each carry their own tour object (grep avoidHit|stepId in site/demo: 1/4/1 hits). Plugin-original module "extracted from three demos". Verdict: keep plugin-only (optionally offer it to the site later).
- capture-demo-frames.mjs: DIVERGED PORT, declared in its header ("PORTED from the practice site's tools/capture-demo-frames.mjs. Changes: 1 CHROME_BIN … 2 --demo/--out/--scenario + file:// … 3 per-demo scene lists gone"). Diff also shows the plugin version adds `assert` steps and exits 1 on any console log or failure; the site version always exits 0 (`LOGS:` printed, then chrome.kill(), no non-zero exit) and hard-codes `/Applications/Google Chrome.app/...` plus the large-document-extraction scenes and MODE=site/frames/script env interface. Site scenario JSONs (tools/capture-wfo-tour.json etc.) use the same step keys (click/sleep/shot/eval/type) (inferred compatible). Verdict: one copy. The plugin's is the superset; keep it plugin-side and have the site retire its fork (site-side change), or keep the site's and delete the plugin's — not both.
- demo-playbook.md vs AO-Personal-OS .claude/references/interactive-demo-playbook.md: same 11 requirements (R1-R11 map 1:1 to personal 1-11, re-ordered: personal #7 figures and #8 model routing are plugin §2/§6), same pitfalls, same procedure. The plugin's is the generalized rewrite (no Alex, no machine paths, "cheaper model/strongest model"). The PERSONAL copy has DRIFTED vs the site: step 9 "Wire: config.js (demoUrl, demoPreviewUrl, videoPoster)" and step 10 "→ demoPreviewUrl" — retired in site round 12 (CONFIG.md §3a "Retired in round 12 … the checker fails any of them"); step 6 "`MODE=script`" is the site fork's interface. AO-Personal-OS CLAUDE.md still sends every session there first ("Read `.claude/references/interactive-demo-playbook.md` first").
- demo-playbook.md vs site HANDOFF-*-demo.md / PROVENANCE §16, §19, §22: the site docs are per-build RECORDS (with customer-named sources, e.g. PROVENANCE §16.1 names the recording's OneDrive customer folder); the playbook distills rules from them. No copy; no drift found in the rules. Site HANDOFF-workforce-demo.md step 4 "no SoftServe / Oracle / NVIDIA marks" is the historical over-generalisation the owner later corrected.
- Intra-plugin contradiction: shared/references/anatomy/artifact-demo.md (loaded at the demo `flow` step): "Name → app chrome only, no vendor or customer marks" and "Oracle products → de-branded connector names". Demo card synthetic-data #1, demo-playbook R2, personal playbook #2 (Alex, 2026-09-16: "not no Oracle marks — no specific customer marks"): vendor and platform marks are expected. -> DRIFTED inside the plugin.
- Site knowledge the demo skill under-specifies: the step-frame spec. handoff card #1 / qa #4 say "cropped to the listing's fixed step-image spec"; the spec lives in the site's ASSETS.md §1 (16:10, 1600×1000 JPEG q86 ≤300 KB, renders ~454 CSS px wide, rendered type ≥12 px, crop to one region). The plugin never names ASSETS.md (manifest docs.assets). The personal playbook does (step 8). -> GAP: should be read from the site at run time via manifest docs.assets.
- Wiring (handoff card, playbook §3 steps 12-14, appendix A4) = CONSISTENT with CONFIG.md §3a / manifest publish.demoTargets. BUT a new walkthrough fails the checker's DEMO_SLUGS (#4) — not mentioned anywhere in the demo skill.

## Part 5 — client-documents.md, three homes
- Plugin shared/references/client-documents.md (77 lines; identical in plugins/oracle-packs-web/shared/ — `diff -rq shared plugins/oracle-packs-web/shared` differs only by tests/) vs AO-Personal-OS .claude/references/client-documents.md (144 lines).
- Same: offers/proposals bullets (de-personalised), living-documents rule, structure-before-copy, headings (both carry the SS26 re-measure: H1 ~15, product name ~22, H2 ~30), name the readers, internal vocabulary, word-frequency.
- Diverged by design: identity (personal: "Alex Orlov + ao@alexorlov.co"; plugin: from the spec's contacts per channel, never a personal mailbox) — plugin is right for practice docs.
- Relocated: problem-statement rule -> shared/references/anatomy/problem-solution.md; business-metrics rule -> anatomy/kpis.md #5 + SPEC025/026 + entry-overview card.
- Missing in plugin (personal only, 2026-09-23/24): "A label earns its words"; peers never open on the same words; "A message read outside the product stands alone"; "A form does its job or says it can't". The plugin header says "Built up from his reviews between 2026-07-07 and 2026-09-16" — a snapshot. The last two concern site-owned email/forms (inferred: not needed by the listing); the first two bite listing copy (case-study metric values feed the home cards; checker enforces some instances).
- Site: START-HERE §4 restates a site-scoped subset (persona first, label earns its words, structure, heading budgets, repetition incl. peers, message stands alone, forms) and CLAUDE.md L17 + START-HERE §4/§10 point to the AO-Personal-OS copy — unreachable for a pilot user (inferred).
- Intra-plugin: client-documents.md has the SS26 heading numbers, while the listing copy-rules card / listing-rules B4 still carry the archive "≤ ~24" numbers.

## FINAL ANSWER (as handed back)
See the SubagentHandback message; identical text.

**Headline.** The listing skill restates most of the site's contract, and 8 of those restatements have drifted, although the skill's "written against site round 14" matches the site's round 14. So comparing rounds does not catch drift. Two stored copies can go: the exemplar (current today) and reference-demo/ (identical to the site's copy). The deny-list converter writes a file the site checker never reads.

(a) See table in the handback. (b) Minimal footprint. (c) Risks. Evidence for every row is in Parts 1-5 above.
