# Packaging skills — shippable skills for the Oracle pack artifacts

_status: **built 2026-09-18.** A standalone private repo — `alexey-orlov/Oracle-Packaging-Skills`, local `~/Documents/GitHub/Oracle-Packaging-Skills` — that is a Claude Code plugin marketplace with two plugins: **`oracle-packs`** (`spec` · `feature-list` · `deck` · `one-pager` · `exec-summary` · `build`) and **`oracle-packs-web`** (`listing` · `demo`), over a shared reference/data/schema/linter bundle and a Workforce-Optimization worked example. The end-to-end test is green on all four document builders. Both research blockers are closed by [Alex's 2026-09-18 decisions](docs/2026-09-17_packaging-skills-prep/decisions-2026-09-18.md) — **12 components**, and a PoV of **4–8 weeks with a 10-week hard cap**. **Brand-checked 2026-09-18** against SoftServe's rebrand (see [oracle-packs](oracle-packs.md)): the deck kit was already on the 2026 brand, three breaks in `oracle-packs-web:listing` were fixed, and a short list stays open (below). **Two human checks remain and neither is done:** the 30-minute plugin spike (the `${CLAUDE_PLUGIN_ROOT}` indirection is unproven — everything was run from the repo root with explicit paths) and an interactive walk of `/oracle-packs:spec` on a real case, the Large docs pack being the natural first._
_updated: 2026-09-18_
_source files: SoftServe OneDrive `Projects/Oracle/Packs/` → [registry](../../_meta/external-sources.md)_

## Snapshot

- **What this is:** the meta-layer over [oracle-packs](oracle-packs.md) — instead of hand-building each pack's artifacts, Alex's packaging method and his accumulated review corpus are packaged as reusable skills. Its own origin rule is the one he gave Vlad on 2026-07-24: describe post-factum the steps the analytical work actually consisted of, cluster them into 2–3 skills, and write them, so each engagement "gives birth to self-acceleration skills" ([sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md)).
- **The spine moved from the feature table to a spec file.** Alex's 07-24 rule (*finish the table's contents; formatting is a separate later pass*) is now executed as: `spec` runs an interactive intake — question widgets, generalization research, name / one-liner / problem↔solution option sets — and signs off the **12 components one at a time in a fixed order** (problem↔solution → one-liner → ICP → name → verticals → capabilities → workflow → architecture → Oracle products → KPIs → packages, then a brief confirmation). Every builder reads that one `pack-spec.yaml`; the feature list stays the master *feature* view and the mini-site's workflow-stage view is derived from it per pack.
- **Two audiences, not one** — R&D (Vlad and team) run Claude Code, have Node and Chrome and can install a plugin; SoftServe sales/presales largely do not run Claude Code at all, so for them the deliverable is the artifact ([D §5](docs/2026-09-17_packaging-skills-prep/D-context-inventory.md)). Hence the split: **`oracle-packs` is python-pptx/docx only** (a low enough bar for a technical presales person), **`oracle-packs-web`** needs Node + Chrome + the site repo. Vlad's pilot is a **plain skills copy** before any marketplace install.
- **The standard artifact set is six, not five** — feature list · sales deck · sales one-pager · interactive demo · mini-site listing · **executive-summary slide** (built on the host deck's own master). The **demo video and the Oracle Marketplace package/listing stay part of a pack's end state but are deliberately not built by the skills**; Vlad's internal *scoping* one-pager is not a standard artifact at all.
- **Plugin names moved at build time** — the 09-18 decision wrote `pack-docs` / `pack-web`; what shipped is `oracle-packs` / `oracle-packs-web`, so the commands read `/oracle-packs:spec`.

## What shipped (2026-09-18)

- **Eight skills across two plugins**, flat (Claude Code scans exactly one level deep): `spec` (intake → signed-off spec) · `feature-list` (.docx) · `deck` (10-slide .pptx on the SoftServe base, with a fit report) · `one-pager` (HTML → one A4 PDF, pushing back on overflow) · `exec-summary` (one slide on the host deck's master, `--channel internal|partner_print`) · `build` (all of them in order, with a review pause after each); `listing` (a `products[]` entry for the mini-site, run through the site checker) · `demo` (interactive walkthrough, sources asked for first).
- **Shared bundle** (`shared/`): references — `engagement-context` · `naming-and-clearance` · `pack-anatomy` (12 components × 6 artifacts) · `pov-rules` · `review-loop` · `packaging-coaching-rules` (**19 rules distilled from the [2026-07-24 Vlad sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md)**) · `slide-design` · `client-documents` · `research-standards`. Schema: `pack-spec.md`.
- **Data, so no pack invents its own facts:** `oracle-products.yaml` — **55 canonical Oracle/NVIDIA entries verified against vendor pages**, the shared catalog Alex asked for (*"Oracle Fusion Field Service"* is the correct name; **"AIDP" is never written** in partner-facing copy) — plus a roadmap extract (**91 items with minted stable ids · 26 L2 patterns · a 9-row pack crosswalk · an 18-row tracker snapshot · a regeneration script**), which is how the churning pack ↔ use-case-map join ships versioned instead of embedded. **Regenerated 2026-09-22** against the map's [six-block re-grouping](oracle.md) — the ids, items, statuses and L1/L2 patterns are unchanged, so only the replaceable `block` label moved; the extract's README now carries the six blocks plus the two unpackaged families, and states that `l1_pattern`/`l2_pattern` is the durable taxonomy while `block` is a display label no skill may hard-code.
- **Linters + tests:** `lint_spec.py`, `lint_artifact.py` (clearance, naming, banned vocabulary, prices), `check_consistency.py`, `packlint.py`, a deny-list and `tests/run_tests.sh` — **65 assertions, green**. This generalizes the mini-site's customer-name/pricing deny-list into a check that runs over *every* produced artifact, which the [D inventory](docs/2026-09-17_packaging-skills-prep/D-context-inventory.md) named as one of the three risks that actually bite.
- **Worked example:** `examples/workforce-optimization/pack-spec.yaml` — ~840 lines, `status: draft`, **16 open questions**, and four sign-off gaps `lint_spec.py --strict --signoff` prints by design (its first-order components cite artifacts, not `user:<date>`, because the pack owner has not walked it through `/oracle-packs:spec`).
- **Its own docs:** `docs/DECISIONS.md` · `docs/PLAN.md` · `docs/TEST-REPORT.md` (the end-to-end pass: four document builders clean on the example, listing tools verified, leak scan clean, both plugin manifests and the marketplace valid) · `docs/packaging-approach.html`, a two-screen explainer of the packaging approach.

## Alex's design decisions — 2026-09-18

Full table: [decisions-2026-09-18.md](docs/2026-09-17_packaging-skills-prep/decisions-2026-09-18.md). The ones that bind generated artifacts:

- **Tiers: one vocabulary everywhere — PoV Jumpstart / Integration / Scaling**; S/M/L survives only as size tags in internal tables, and print artifacts get relabelled at their next rebuild.
- **One metric set per pack**, from the customer study, with attribution varying by approval and channel (under the logo where approved, anonymized otherwise). **Two contradicting or overlapping metric sets for one pack are unacceptable.**
- **Features: Area > Category > Feature with ● available / ◐ partial / ○ roadmap** — one glyph system, ending the two incompatible ones the delivered artifacts carry.
- **Optional Oracle products are a required/optional flag per item inside the architecture stack** (required = built on it or fully relying on it; optional = a plausible additional source or destination) — the shape no delivered artifact had, plus the shared catalog it must be picked from.
- **Pack name by channel:** mini-site "Workforce Optimization" · internal exec slide "Workforce Optimization App" · external one-pager and deck "Workforce optimization" with an *Accelerator App by SoftServe* subheading if needed.
- **Every integration claim states its tier** — WfO: file export/import at PoV, API integration at Integration tier (which is why the one-pager and the feature list are both right).
- **One A4 page for the sales one-pager, always** — the skill pushes back and proposes cuts rather than spilling to a second page. **Contacts as today:** one-pager and deck → Karsten (`ktram@`), mini-site → `oracle@softserveinc.com` with Karsten named, internal deck → the packager plus `RnDrequest@`.
- **The July WfO build sessions are unrecoverable** — not on this Mac, not in the cloud, not in claude.ai history; only the Jul-24 Vlad recording exists. Those learnings stay second-hand (this wiki, the call note, the July-dated reference rules). **The WfO artifacts are not being rebuilt in this project** — the goal is the skills.

## Rules the skills encode (the durable ones)

The canonical home is now `shared/references/` in the repo; the derivations stay in the prep corpus. What must not drift:

- **Never invent a tier, a price or a proof point.** A pack with no S/M/L gets the block reframed ("what the PoC buys" — real duration, real contract value, IN / NOT IN SCOPE). Peer claims are all-or-none; "PROVEN" is reserved for delivered results. **Prices come from the real signed SoWs**, services separated from client-paid consumption. ([A1 §3.1](docs/2026-09-17_packaging-skills-prep/A1-sessions-decks-onepagers.md) · [C R29](docs/2026-09-17_packaging-skills-prep/C-vlad-feedback.md))
- **Clearance is per-artifact and revocable** — a customer name cleared in one round was withdrawn two days later; nothing that must never ship lives inside the publish root ("unreferenced is not unshipped"). ([A2 §3c](docs/2026-09-17_packaging-skills-prep/A2-sessions-minisite-demos.md))
- **Feature-table row rules**, the closest thing to a spec for `feature-list`: rows in **workflow order** · a feature name points at **the task being solved** · **never name the model** · the custom-work column means what stays custom *after* the feature is finished · granularity follows where the product can extend · merge conceptually identical steps and turn the surviving distinction into a real feature · untested is not a capability · status claims get verified against the delivery · every step's nuance space gets enumerated, each unhandled nuance becoming a row with an empty circle. ([07-24 sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md))
- **Counted allowances with worked examples** in every open-ended tier cell, scope decomposed on a fixed delivery-layer taxonomy so two packs are comparable; **architecture ≠ capability** (layers visualized as layers, features as a structured list); a generated one-liner states the job and the outcome in the reader's words, never the packaging tagline. ([C R11, R27–R28](docs/2026-09-17_packaging-skills-prep/C-vlad-feedback.md))
- **Process:** tables before slides · red-team the candidate set · gap handling is an approval gate, not an improvisation · one structure applied identically to every pack · a dedicated editorial/fact-check pass at the end · verify stated counts against the file before building to them. ([A1 §3.2, §3.5](docs/2026-09-17_packaging-skills-prep/A1-sessions-decks-onepagers.md))
- **Model routing expressed as roles, not model names** — cheapest model for mechanical work, strongest for design and red-team calls — or the skill ships Alex's subscription shape. ([A2 §2.7](docs/2026-09-17_packaging-skills-prep/A2-sessions-minisite-demos.md))

## Still rough (from `docs/TEST-REPORT.md` §10)

- **Deck slide 4's two screenshot slots are dashed empty containers** ("Before: the manual artefact" / "After: the product screen") — correct as empty instances, but no deck goes to a seller with them.
- **The packages table type-scales to 84%** to fit its band — inside the anatomy's allowance, but it is type shrinking to fit content, which the design rules otherwise forbid; the real fix is a wider tier column or fewer rows.
- **`deckkit.py` is duplicated** in the `deck` and `exec-summary` tool dirs (byte-identical today) — the next one-sided edit is a silent divergence.
- **The one-pager is height-bound, not word-bound**, so "cut words" can fail to fix an overflow while a shorter architecture label fixes it; the skill's guidance still talks only about word budgets. **Chrome is spawned per run** and can outlive a killed build.

## People

- **Vladyslav Butenko** — the BA/PdM Alex coaches on pack productization and the pilot user for the plain skills copy; the [07-24 sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md) is the only verbatim record of that coaching → [people page](../../people/vladyslav-butenko.md).
- **Gero Gunkel** (Oracle CTO) — co-designed the tier model the skills emit → [oracle-team.md](oracle-team.md).

## Decisions

- 2026-09-18 — **The twelve open design questions are settled** (component count 12 · one tier vocabulary · PoV 4–8 weeks with a 10-week cap · one metric set per pack · Area>Category>Feature with three glyphs · optional Oracle products as flags off a shared catalog · name by channel · integration claims state their tier · exec summary is a standard artifact while demo video and Marketplace are not · one A4 one-pager · contacts as today · standalone marketplace repo with a plain-copy pilot for Vlad). ([decisions](docs/2026-09-17_packaging-skills-prep/decisions-2026-09-18.md))
- 2026-09-18 — **The Jul-17 WfO sales one-pager is canonical** and the Jul-13 PDF in the OneDrive pack folder was **overwritten in place under the same name so the share link survives**, the old file kept locally as "(2026-07-13, superseded)" → [oracle-packs](oracle-packs.md). Closes the five-values-for-one-cell contradiction the artifact map surfaced.
- 2026-07-24 — **Every engagement should end in self-acceleration skills**, named post-factum from the steps the analytical work actually consisted of. Alex set this for Vlad; it is the mandate for this subproject, and Vlad acted on it — his 2026-09-10 Account Insights one-pager adds a fourth matrix column, *"artifact to accelerate the development"*, naming ten reusable assets. ([sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md) · [C R7](docs/2026-09-17_packaging-skills-prep/C-vlad-feedback.md))

## Brand alignment after SoftServe's 2026 rebrand (audited 2026-09-18)

- **The deck kit was already right, and that was the biggest risk.** `softserve-deck-base.pptx`'s theme names **Azurio** + **Replica LL TT** with the Lviv-blue / Austin-orange scheme; there is no teal, no Montserrat and no Open Sans anywhere in the repo, and no rounded shapes or shadows in the package. `docProps` identifies it as SoftServe's own **2026 Corporate Template** (modified 2026-07-27), which also settles a question the web spec could not: **the spark monogram is not retired** — it is the only media in SoftServe's current template. It is retired from the *web header*, not from the brand.
- **Three breaks fixed in `oracle-packs-web:listing`** (commit `b2bfa28`): the publish wrapper stripped `data-theme="dark"` by exact line and the site's page now opens `data-theme="light" data-brand="ss26"`; the skill knew nothing about sentence case, so a newly inserted product would have shipped in CAPITALS (it now says: write every string in sentence case, and never add rows to the site's one-time `content-case.js` overlay); and `assets/README.md` pointed at the skill's own fork of `check-grammar.js`, which has no brand assertions and would pass a listing the site's gate rejects.
- **Hexes deliberately left alone.** Every SoftServe colour in the kit is one channel step from the website's tokens (`1485C3`/`1485C4`, `F36949`/`F46A4A`, `26282B`/`26292B`) — but those values came out of SoftServe's *own EMEA template*, so "correcting" them to web values would make decks diverge from the master. Both sets are now recorded in `.claude/references/softserve-deck-kit.md`, with the template's true-grey ramp vs the web's blue-tinted one flagged as unresolved.
- **The trap worth knowing:** the deck base's layout defaults its title to `cap="all"` + `+mn-lt`, i.e. UPPERCASE Replica — the pre-rebrand convention. Only the base's own slide override makes it sentence-case Azurio, so every new title run has to carry `cap="none"` + `+mj-lt` explicitly.

## Open loops

**Mine**
- **Run the 30-minute plugin spike** — copy `plugins/oracle-packs/` into a test repo's `.claude/skills/`, start a session at that root, confirm `/oracle-packs:spec` appears and `${CLAUDE_PLUGIN_ROOT}` resolves (read `shared/data/oracle-products.yaml`, run `shared/tools/lint_spec.py` through it); `tools/sync-shared.sh` first. Until it passes, the marketplace shape is unproven.
- **Walk `/oracle-packs:spec` interactively on a real case** — the Large docs pack. `spec` is the one skill with no executable surface: its questions, research fan-out, option triads and component sign-off were read for consistency, never exercised. Only a real run shows whether the flow is bearable at twelve components.
- **Clear `softserve-deck-base.pptx` for distribution** (licence / branding posture) before the repo leaves the org boundary.
- **Export the missing Vlad/Oracle transcripts**, in priority order: later occurrences of the recurring "Oracle Productization & BA JumpStart Sync" series (where the Sep-10 artifacts were reviewed), the 2026-07-06 Butenko onboarding, the 2026-06-12 "T-Shirt Packaging approach discussion" (304 MB, never transcribed), and any Teams chat carrying written feedback on Vlad's one-pagers.
- **Answer the go/no-go criterion question** the `spec` gate needs: Gero parked Riyadh Air as unpackageable on 2026-06-26 and it became pack 2 anyway — what changed is the criterion.

**Theirs**
- **Vlad** — take the plain skills copy for the pilot; his own skills folder ("in my new projects in Claude it converts easily into skills; I just don't yet keep a skills folder", 2026-07-24); the pack demo videos still open on the tracker → [oracle-packs](oracle-packs.md).

## Activity

- 2026-09-18 — **Brand-impact audit after the mini-site's rebrand** — the kit was already on the 2026 brand; three `listing` breaks fixed, the heading budget restated for sentence-case display type across `shared/references/client-documents.md` (and its two synced copies), and `.claude/references/softserve-deck-kit.md` / `slide-design.md` corrected on the deck-vs-web hexes, the `cap="all"` trap, the spark, and blue-not-orange as the action colour. Open: the orphaned `docs/packaging-approach.html` (the complete retired brand), the one-pager's uppercase headings / bold-sans H1 / rounded corners, re-deriving the deck base, and re-porting the skill's `check-grammar.js`. (chat, 2026-09-18)

- 2026-09-18 — **The skills were built** as `alexey-orlov/Oracle-Packaging-Skills`: two plugins (8 skills), the shared reference/data/schema/linter bundle, the 55-entry Oracle product catalog, the versioned 91-item roadmap extract, 65 green test assertions, a WfO worked example and an end-to-end test report (`docs/TEST-REPORT.md`). Alex's twelve design [decisions](docs/2026-09-17_packaging-skills-prep/decisions-2026-09-18.md) landed the same day, closing both research blockers; the July WfO build sessions were confirmed unrecoverable.
- 2026-09-17 — **Four research passes + a recovery handoff committed as the prep corpus**: [A1](docs/2026-09-17_packaging-skills-prep/A1-sessions-decks-onepagers.md) (seven deck/one-pager sessions, Alex's decision-widget answers verbatim, a consolidated rulebook) · [A2](docs/2026-09-17_packaging-skills-prep/A2-sessions-minisite-demos.md) (five mini-site/demo sessions, the 23 listing + 16 demo rules, the `products[]` and demo data shapes) · [B](docs/2026-09-17_packaging-skills-prep/B-artifact-component-map.md) (artifact↔component map read off the files, canonical wording, 12 ambiguities) · [C](docs/2026-09-17_packaging-skills-prep/C-vlad-feedback.md) (29 feedback rules, the 12-step method, the S/M/L model's origin) · [D](docs/2026-09-17_packaging-skills-prep/D-context-inventory.md) (the ~450 KB shippable bundle, delivery options, 14 portability risks) · plus the [partial 07-24 transcript](docs/2026-09-17_packaging-skills-prep/vlad-2026-07-24-transcript-partial.md) and the [July WfO recovery handoff](docs/2026-09-17_packaging-skills-prep/HANDOFF-july-wfo-sessions.md), now closed as unrecoverable.
- 2026-07-24 — [the full Vlad productization sync](calls/oracle/2026-07-24_133119_one-on-one_vlad-productization-sync.md) (folded 2026-09-18) — the whole 31 minutes, superseding the partial transcript: the 0:35–27:02 row-by-row walk of the capability table is where most of the feature-list rules come from, and it is the source of the repo's 19 packaging-coaching rules.
